name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v7

    - name: Install uv
      uses: astral-sh/setup-uv@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: uv sync --extra dev

    - name: Run pre-commit
      run: uv run pre-commit run --all-files

    # pre-commit's mypy hook is scoped to src/ because its isolated env
    # lacks the scripts' imports; this run covers src/ and scripts/ both,
    # using the project env where those imports resolve.
    - name: Type-check src/ and scripts/
      run: uv run mypy

    - name: Check for duplicated code blocks
      run: >
        uv run --with pylint python -m pylint --disable=all
        --enable=duplicate-code --min-similarity-lines=10
        --ignore-imports=yes src/esolangs scripts tests

    - name: Verify the single-interpreter installer
      run: uv run python scripts/verify_install_one.py

    # Re-derives the ZTOALC anchor table and diffs it against the committed
    # file, catching a stale table or a hand-edit.  The pytest suite only
    # looks values up in the table, so it cannot catch either.  Runs here
    # rather than at push time: verify.py leaves it to CI.
    - name: Check the ZTOALC anchor table is reproducible
      run: uv run python scripts/make_ztoalc_table.py --check

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]

    steps:
    - uses: actions/checkout@v7

    - name: Install uv
      uses: astral-sh/setup-uv@v7

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v7
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: uv sync --extra dev --python ${{ matrix.python-version }}

    # Coverage is only consumed from the 3.12 leg (Codecov upload, the badge
    # artifact), and `coverage` cannot use its sysmon core before 3.12 --
    # sys.monitoring is 3.12+.  On 3.10/3.11 it falls back to the settrace
    # tracer, which fires per executed line and costs ~3x on interpreters that
    # are tight dispatch loops.  So measure on 3.12 and just run the tests
    # everywhere else.
    - name: Test with pytest (with coverage)
      if: matrix.python-version == '3.12'
      # The local default is a fixed, modest worker count so developer laptops
      # keep cores free; CI has the box to itself, so ask for all of them.
      run: uv run pytest -n auto --cov --cov-report=xml --cov-report=term-missing

    - name: Test with pytest
      if: matrix.python-version != '3.12'
      run: uv run pytest -n auto

    - name: Upload coverage to Codecov
      if: matrix.python-version == '3.12'
      uses: codecov/codecov-action@v7
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

    - name: Upload coverage report for the badge
      if: matrix.python-version == '3.12'
      uses: actions/upload-artifact@v7
      with:
        name: coverage-xml
        path: coverage.xml
        if-no-files-found: error
        retention-days: 1

  coverage-badge:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: test
    steps:
    - uses: actions/checkout@v7

    - name: Download coverage report
      uses: actions/download-artifact@v8
      with:
        name: coverage-xml
        path: .

    - name: Install uv
      uses: astral-sh/setup-uv@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: uv sync --extra dev

    - name: Regenerate the coverage badge
      run: |
        uv run python scripts/make_coverage_badge.py
        if ! git diff --exit-code docs/coverage-badge.svg; then
          echo "docs/coverage-badge.svg is out of date. Run:"
          echo "  uv run pytest --cov --cov-report=xml"
          echo "  uv run python scripts/make_coverage_badge.py"
          exit 1
        fi

    - name: Regenerate the language capabilities doc
      run: |
        uv run python scripts/make_languages_doc.py
        if ! git diff --exit-code docs/languages.md; then
          echo "docs/languages.md is out of date. Run:"
          echo "  uv run python scripts/make_languages_doc.py"
          exit 1
        fi

  security:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v7

    - name: Install uv
      uses: astral-sh/setup-uv@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: uv sync --extra dev

    - name: Run bandit security check
      run: uv run --with bandit bandit -r src -f json -o bandit-report.json

    - name: Audit dependencies with pip-audit
      run: uv run --with pip-audit pip-audit --local

  line:
    # extra/line is a standalone spike: its modules import each other as flat
    # top-level names, and it needs image libraries the package itself does
    # not depend on, so it is not under the root `tests/` testpaths and is run
    # here from its own directory with those deps supplied ad hoc.
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v7

    - name: Install uv
      uses: astral-sh/setup-uv@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: "3.12"

    - name: Run the extra/line suites
      run: |
        cd extra/line
        uv run --isolated --no-project \
          --with pillow --with numpy \
          --with pytest --with pytest-xdist \
          pytest . -q

    # Line is not in registry.py (its programs are PNGs), so these __main__
    # blocks are the only way to run it.  The pytest suites import the
    # functions directly and never execute them, so a broken argv index or
    # entry point would otherwise go unnoticed until someone ran it by hand.
    - name: Smoke test the extra/line entry points
      run: |
        set -e
        cd extra/line
        run_line() {
          uv run --isolated --no-project \
            --with pillow --with numpy \
            python "$@"
        }
        run_line render.py "+++" /tmp/line_render.png
        [ "$(run_line simulate.py /tmp/line_render.png)" \
          = "final tape (nonzero cells): {0: 3}" ]
        run_line extract.py /tmp/line_render.png >/dev/null
        run_line bf_to_line.py "+++." /tmp/line_bf.png >/dev/null
        [ "$(run_line simulate.py /tmp/line_bf.png)" \
          = "3
        final tape (nonzero cells): {0: 3}" ]
        run_line line_boolean.py 0001 /tmp/line_bool.png >/dev/null
        run_line verify.py >/dev/null
        echo "All extra/line entry points passed"

  assembly:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v7

    - name: Build and run the RISC-V reference interpreters
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-riscv64-linux-gnu qemu-user
        for s in extra/assembly/*-riscv.s; do
          base=$(basename "$s" .s)
          riscv64-linux-gnu-gcc -nostdlib -static -march=rv64i -mabi=lp64 \
            -o "/tmp/$base" "$s"
          echo "Built $s"
        done
        # the reference interpreter must execute and halt; feed each a valid
        # program (an empty stdin is malformed for some, e.g. BF-PDA, and
        # would exit nonzero under `set -e`)
        printf 'o' | qemu-riscv64 /tmp/nocomment-riscv >/dev/null
        printf '.' | qemu-riscv64 /tmp/bfpda-riscv >/dev/null
        printf 'A' | qemu-riscv64 /tmp/ram0-riscv >/dev/null
        printf '0ox;' | qemu-riscv64 /tmp/bio-riscv >/dev/null
        printf '+' | qemu-riscv64 /tmp/minsky_swap-riscv >/dev/null
        echo "All extra/ RISC-V interpreters ran natively"

    - name: Run the RISC-V reference interpreters under unicorn
      run: |
        python3 -m pip install -q unicorn
        # the differential (Python vs the RISC-V references, corpus + fuzz)
        # needs unicorn and the RISC-V compiler.
        PYTHONPATH=src python3 scripts/verify_differential.py --fuzz 50 --seed 1
        # the reference interpreter must execute and halt; feed each a valid
        # program (an empty stdin is malformed for some, e.g. BF-PDA, and
        # would exit nonzero under `set -e`)
        printf 'o' | python3 scripts/riscv_elf_runner.py /tmp/nocomment-riscv \
          >/dev/null
        printf '.' | python3 scripts/riscv_elf_runner.py /tmp/bfpda-riscv \
          >/dev/null
        printf 'A' | python3 scripts/riscv_elf_runner.py /tmp/ram0-riscv \
          >/dev/null
        printf '0ox;' | python3 scripts/riscv_elf_runner.py /tmp/bio-riscv \
          >/dev/null
        printf '+' | python3 scripts/riscv_elf_runner.py /tmp/minsky_swap-riscv \
          >/dev/null
        echo "All extra/ RISC-V interpreters ran under unicorn"

    - name: Compile and run the RISC-V compilers' output
      run: |
        # each assembly compiler's output is assembled and executed under
        # unicorn, so the compilers are verified end to end, not just that
        # they emit assembly
        PYTHONPATH=src python3 scripts/verify_riscv_unicorn.py
        echo "All RISC-V compilers and references round-tripped under unicorn"

    # TestDivergenceDetection tampers with a native reference and asserts the
    # fuzzer reports the divergence -- the tests that establish the
    # differential fuzzer (and the weekly fuzz job) can catch a bug at all.
    # Their skipifs need the RISC-V toolchain, which the `test` matrix job
    # lacks, so they skipped on every run there.  This job has it.
    - name: Install uv
      uses: astral-sh/setup-uv@v7

    - name: Install dependencies
      run: uv sync --extra dev

    - name: Run the fuzzer's divergence-detection tests
      run: |
        # a skip here means a toolchain regressed and the tests silently
        # stopped verifying anything, so fail on one rather than report green
        uv run pytest tests/fuzz/test_differential_fuzz.py -m slow \
          -o addopts= -q -rs --no-header | tee /tmp/divergence.log
        if grep -qE '[0-9]+ skipped' /tmp/divergence.log; then
          echo "::error::divergence-detection tests skipped; toolchain missing"
          exit 1
        fi

  # Disabled for now.  Re-enable by deleting the `if: false` line below;
  # nothing else in the workflow needs this job.
  #
  # When it does run, `lake build` builds the `Esolangs` root, which imports
  # every proof module, so the job checks all of them.  Keep new proofs
  # imported from `Esolangs.lean` -- one that is not is built by nothing.
  # (That was how `BfMintermCorrect` rotted into 35 elaboration errors
  # unnoticed: the root once imported every proof, and when those imports were
  # dropped the default target quietly narrowed to MAMMALIAN alone.)
  #
  # The cache key and `sources_hash` below enumerate `Esolangs.lean` and
  # `Esolangs/*.lean`, which covers the current layout; a proof added outside
  # those paths would be invisible to both.
  lean:
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v7

    - name: Install elan
      run: |
        curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
          -o /tmp/elan-init.sh
        sh /tmp/elan-init.sh -y --default-toolchain none
        echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

    - name: Cache mathlib build
      uses: actions/cache@v6
      with:
        path: extra/lean/esolangs/.lake
        key: lean-esolangs-${{ hashFiles('extra/lean/esolangs/lake-manifest.json') }}-${{ hashFiles('extra/lean/esolangs/lean-toolchain') }}-${{ hashFiles('extra/lean/esolangs/lakefile.toml', 'extra/lean/esolangs/Esolangs.lean', 'extra/lean/esolangs/Esolangs/*.lean') }}
        restore-keys: |
          lean-esolangs-

    - name: Build the Lean proofs
      run: |
        cd extra/lean/esolangs
        sources_hash=$(sha256sum lakefile.toml lake-manifest.json lean-toolchain Esolangs.lean Esolangs/*.lean | sha256sum | cut -d' ' -f1)
        if [ -f .lake/build/lean-sources.hash ] && [ "$(cat .lake/build/lean-sources.hash)" = "$sources_hash" ]; then
          echo "Lean sources unchanged; skipping lake build"
        else
          lake build
          echo "$sources_hash" > .lake/build/lean-sources.hash
          echo "Lean proofs built"
        fi
